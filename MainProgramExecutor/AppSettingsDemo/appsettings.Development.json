/*
  This file gets checked into source control (not ignored in github). Overrides all settings that might be sensitive.
  Sending e-mail is not something you want to do in development.
*/

/*
  We would put sensitive data in an appsettings.Production.json file in this same directory.
*/

//team development server environment data gets sstored here.

{
  "DetailedErrors": true,
  "MySetting": "This is from appsettings.Development.json, this value overrides whatever is in appsettings.json",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
